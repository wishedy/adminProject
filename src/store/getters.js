const  getters = {
  toggleOnOff(state){
      return state.toggleOnOff;
  },
    nowTabData(state){
      return state.nowTabData;
    },
    Context(state){
      return state.Context;
    }
};
export default  getters;
